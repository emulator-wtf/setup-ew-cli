{
  "name": "setup-ew-cli",
  "version": "0.9.10",
  "description": "Github action to setup ew-cli in a workflow",
  "scripts": {
    "build": "rimraf dist && tsc"
  },
  "homepage": "https://github.com/emulator-wtf/setup-ew-cli",
  "license": "MIT",
  "dependencies": {
    "@actions/core": "^1.11.1",
    "@actions/exec": "^1.1.1",
    "@actions/tool-cache": "^2.0.2"
  },
  "devDependencies": {
    "@types/node": "^20.19.25",
    "typescript": "^5.9.3",
    "rimraf": "^6.1.2"
  }
}
